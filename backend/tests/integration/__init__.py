"""Integration tests.

Real PostgreSQL/TimescaleDB and Redis via testcontainers. Empty at S01: the
repository skeleton owns no infrastructure to integrate with. Populated by S04
(Persistence Foundation) and S05 (Event Bus & Job Runtime).
"""
